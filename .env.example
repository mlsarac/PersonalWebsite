# Canlı site adresi: https ile, sonda / olmadan
# Yerelde kullanmak için kopyala: copy .env.example .env   (Windows)
# Netlify’da genelde gerekmez — build’de URL ortam değişkeni otomatik gelir.
VITE_SITE_URL=https://orneksite.com
